export interface Localization {
    latitude: number;
    longitude: number;
    latitudeDelta: number;
    longitudeDelta: number;
}

export interface Store {
    display_name: string;
    lat: string;
    lon: string;
}